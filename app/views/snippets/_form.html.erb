<%= form_for(@snippet) do |f| %>
  <% if @snippet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@snippet.errors.count, "error") %> prohibited this snippet from being saved:</h2>

      <ul>
      <% @snippet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :language %><br />
    <%= f.select :language, languages, { :include_blank => true } %>
  </div>
    <div class="field">
    <%= f.label :source_code %><br />
    <%= f.text_area :source_code ,:cols => 50, :rows => 20%>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description ,:cols => 50, :rows => 8%>
  </div


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
