<div class="span8">
<b><%=@user.email%></b> 共发布 <%=@user.snippets_count %> 篇代码
  <% @snippets.each do |snippet| %>
    <h2><%=link_to snippet.name,snippet %></h2>
    <p>语言类别：<%=link_to snippet.lan_name,snippet.category %> &nbsp;&nbsp;<%= link_to snippet.author, snippet.user %> </p>
    <p><%= snippet.description %> </p>
    <p>
      <%= snippet.created_at.strftime("%Y-%m-%-d %H:%M:%S") %>
      <%  if snippet.is_own? %>
        <%= link_to '编辑', edit_snippet_path(snippet) %>
        <%= link_to '删除', snippet, method: :delete, data: { confirm: '确定删除?' } %>
      <%  end %>
    </p>
    <p><%=link_to "查看详情 »",snippet,:class => "btn" %>
    <% end %>
</div>
